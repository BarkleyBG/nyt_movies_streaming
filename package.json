{
  "name": "nyt-movies-tests",
  "version": "1.0.0",
  "description": "Test suite for the NYT Top 100 Movie Tracker",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:watch": "jest --watch",
    "test:integration": "playwright test",
    "test:python": "python -m pytest tests/python/ -v",
    "test:all": "npm run test && npm run test:python && npm run test:integration"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.0",
    "jest": "^29.7.0"
  }
}
